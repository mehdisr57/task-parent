spring:
  application:
    name: task-service
  jpa:
    hibernate:
      ddl-auto: update
    show-sql: false
    database: mysql
    open-in-view: false
  datasource:
    username: root
    url: jdbc:mysql://localhost:3306/pooyabyte
    driver-class-name: com.mysql.cj.jdbc.Driver
  batch:
    initialize-schema: always
    job:
      enabled: false
server:
  port: 8087


